package util;

import entityBeans.Autor;
import entityBeans.Categoria;
import entityBeans.Cidade;
import entityBeans.Comentario;
import entityBeans.Editora;
import entityBeans.Endereco;
import entityBeans.Estado;
import entityBeans.Estante;
import entityBeans.Estoque;
import entityBeans.Idioma;
import entityBeans.Livro;
import entityBeans.Resenha;
import entityBeans.Reserva;
import entityBeans.StatusLivro;
import entityBeans.Usuario;

public class SimpleEntityFactory {
	
	public ObjectComboboxIF createEntity(String tipo) {
//		if (tipo == "Autor") 
//			return new Autor();
		if (tipo == "Categoria") 
			return new Categoria();
//		if (tipo == "Cidade") 
//			return new Cidade();
//		if (tipo == "Comentario") 
//			return new Comentario();
//		if (tipo == "Editora") 
//			return new Editora();
//		if (tipo == "Endereco") 
//			return new Endereco();
//		if (tipo == "Estado") 
//			return new Estado();
//		if (tipo == "Estante") 
//			return new Estante();
//		if (tipo == "Estoque") 
//			return new Estoque();
//		if (tipo == "Idioma") 
//			return new Idioma();
//		if (tipo == "Livro")
//			return new Livro();
//		if (tipo == "Resenha")
//			return new Resenha();
//		if (tipo == "Reserva")
//			return new Reserva();
//		if (tipo == "StatusLivro")
//			return new StatusLivro();
//		if (tipo == "Usuario")
//			return new Usuario();
		
		return null;
	}
}
